# Allowlist closure — terminal assisted calibration

## Human gate completion

The Rule-3 checklist generated by this run contains zero items: I1 is
`not_qualified` (0 unique exclude DOIs carry a reproduced I1 hard-fail in
the terminal 40-case corpus). Per the preregistered terminal policy, no
criterion other than I1 could extend the allowlist, and no sample means
nothing for a human to confirm.

**Final allowlist: EMPTY.** No criterion is authorized for automatic
exclusion under the terminal configuration (v3 prompt
`b723fbb0…`, criteria `41ddfe01…`, deepseek-reasoner alias served by
deepseek-v4-flash, n=2 replicates, reproduction required).

Human sign-off: Cedric (Kero-CK), 2026-07-16. — [X] à cocher par Cedric

## What this means in production

- Every record routes to a human. The machine contribution is the
  criterion-level pre-assessment: per-criterion status, exact citations,
  coherence checks, and replicate agreement — decision support and audit
  trail, not decision-making.
- The v1 sealed allowlist ({I1}) is void: it was bound to the v1 prompt
  configuration, which the terminal phase replaced with the v3 prompt.
- Assisted proposals (`needs_human_validation` on reproduced I1) remain
  architecturally possible; the terminal corpus produced zero.

## Engine closure

Per the terminal preregistration (`prompt_optimization_closed: true`) and
the project roadmap: no v4, no further prompt iteration. Reopening the
engine requires a documented production need. Two questions are parked,
documented, and untested:

1. `deepseek-v4-pro` (never tested; the v3 two-arm amendment was dropped
   before the v3 run).
2. The protocol boundary exposed by the v3 held-out block D: hybrid
   train-then-prompt methods (blocD_003) and experience/case-study papers
   (blocD_005) where reproduced hard-fails collided with include labels —
   arguably a criterion-wording tension (I4, E1/E4) rather than pure model
   error.

## Honest bottom line

Across v0→terminal: the screening engine is safe (zero auto-include of an
exclude-labeled record in any run; zero unaudited exclusion possible),
fully auditable (every decision carries mechanically verified citations),
and measured (non-determinism quantified and neutralized by replicate
reproduction). Under those honest constraints, automatic exclusion did not
survive measurement: the assisted automation rate is currently 0%. The
engine's production value is the audited pre-assessment, not the
automation.
